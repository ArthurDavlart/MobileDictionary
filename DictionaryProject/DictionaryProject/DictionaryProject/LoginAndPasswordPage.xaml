<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:resources="clr-namespace:DictionaryProject"
             mc:Ignorable="d"
             x:Class="DictionaryProject.LoginAndPasswordPage">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Sign Up" Clicked="OnSignUpButtonClicked" />
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        <StackLayout VerticalOptions="CenterAndExpand">
            <Label  Text="{x:Static resources:Resource.UsernameLabel}"/>
            <Entry x:Name="usernameEntry" Placeholder="{x:Static resources:Resource.UsernamePlaceholder}" />
            <Label Text="{x:Static resources:Resource.PasswordLabel}" />
            <Entry x:Name="passwordEntry" Placeholder="{x:Static resources:Resource.PasswordPlaceholder}" />
            <Button Text="{x:Static resources:Resource.LoginBtn}" Clicked="OnLoginButtonClicked" />
            <Label x:Name="messageLabel" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>